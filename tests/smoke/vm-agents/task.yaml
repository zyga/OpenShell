# SPDX-FileCopyrightText: Canonical Ltd.
# SPDX-License-Identifier: Apache-2.0

summary: Verify agent binaries are present and executable in the VM rootfs
details: |
    Checks that the claude and copilot binaries are installed at the expected
    paths inside the VM rootfs and have executable permissions.

prepare: |
    # Poke the API in case there's another race when it starts up?
    snap run openshell sandbox list

execute: |
    snap run openshell sandbox -vvv create -- sh -c 'test -x "$(which claude)"'

    # Creating a few sandboxes rapidly may sometimes fail on sqlite lock.
    sleep 3

    snap run openshell sandbox -vvv create -- sh -c 'test -x "$(which copilot)"'

restore: |
    snap run openshell sandbox delete --all
